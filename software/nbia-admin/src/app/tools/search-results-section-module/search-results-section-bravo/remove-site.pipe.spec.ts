import { RemoveSitePipe } from './remove-site.pipe';

describe('RemoveSitePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSitePipe();
    expect(pipe).toBeTruthy();
  });
});
