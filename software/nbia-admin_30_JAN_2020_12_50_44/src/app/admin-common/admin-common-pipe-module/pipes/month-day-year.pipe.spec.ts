import { MonthDayYearPipe } from './month-day-year.pipe';

describe('MonthDayYearPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthDayYearPipe();
    expect(pipe).toBeTruthy();
  });
});
